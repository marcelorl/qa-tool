import { createAction } from 'redux-actions'

export const add = createAction('ADD_QUESTION')
export const remove = createAction('REMOVE_QUESTIONS')
export const sort = createAction('SORT_QUESTIONS')
